# Backend - Examen Productos

Este backend usa **SQLite** como base de datos.  
No es necesario instalar XAMPP ni MySQL.  
Toda la información se guarda en el archivo `productos.db`.

Endpoints disponibles:
- GET /productos
- POST /productos
- DELETE /productos/:id
